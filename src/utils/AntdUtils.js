export const getAntdMenuItem = (label, key, icon, disabled, children) => (
    {
        label,
        key,
        icon,
        children,
        disabled
    }
);

export const getAntdSegmentedItem = (label, value, icon) => (
    {
        label,
        value,
        icon
    }
);

export const getAntdSelectItem = (label, value) => (
    {
        label,
        value
    }
);